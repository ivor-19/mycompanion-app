import { moderateScale } from "react-native-size-matters";

export const FONT = {
  xxxl: moderateScale(32), // Very large headings
  xxl: moderateScale(28),  // Headings
  xl: moderateScale(24),   // Sub-headings
  lg: moderateScale(20),   // Section titles
  md: moderateScale(16),   // Body text
  sm: moderateScale(14),   // Secondary text
  xs: moderateScale(12),   // Captions
  xxs: moderateScale(10),  // Smallest labels
};
